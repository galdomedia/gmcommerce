<% title "Pages" %>
<% form_for @search do |f| %>
  <table>
    <tr><td>Title</td><td><%= f.text_field :title_like, :size => 13 %></td></tr>
    <tr><td colspan="2"><%= f.submit "Find" %></td></tr>
  </table>
<% end %>
<table class="index">
  <tr>
    <th><%= order @search, :by => :title %></th>
  </tr>
  <% for page in @pages %>
    <tr>
      <td><%=h page.title %></td>
      <td><%= link_to "Show", admin_page_url(page) %></td>
      <td><%= link_to "Edit", edit_admin_page_path(page) %></td>
      <td><%= link_to "Destroy", admin_page_url(page), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
<%= will_paginate @pages %>
<p><%= link_to "New Page", new_admin_page_path %></p>
