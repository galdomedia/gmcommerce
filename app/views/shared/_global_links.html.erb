<% if current_user %>
 	<strong><%=h current_user.login %></strong> |
	<% if admin? -%><%= link_to t('gmcommerce.links.administration'), admin_root_url %> | <% end -%>
	<%= link_to t('authlogic.links.logout'), user_session_path, :method => :delete, :confirm => t('authlogic.logout_question') %>
<% else %>
	<%= link_to t('authlogic.links.sign_up'), new_account_path %> |
	<%= link_to t('authlogic.links.sign_in'), new_user_session_path %>
<% end %>
